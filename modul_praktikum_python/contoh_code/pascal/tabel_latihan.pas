uses crt; 
function fpangkat(dipangkatkan:real;pangkat:integer):real; 
var  
  (* variabelnya anda tentukan sendiri  *) 
begin 
  (* pahami program tabel.pas dan kemudian lengkapi bagian 
ini*) 
end; 
procedure cetak(x:integer); 
var  
  (* variabelnya anda tentukan sendiri  *) 
begin 
  writeln(‘  x      1/x        x^2      x^3’); 
  writeln(‘-----------------------------------’); 
(* pahami program tabel.pas dan kemudian lengkapi bagian ini*) 
end; 

var 
(* variabelnya anda tentukan sendiri  *) 
begin  
  write(‘Masukkan banyaknya data : ’);readln(x); 
  cetak(x); 
  readkey; 
end.
