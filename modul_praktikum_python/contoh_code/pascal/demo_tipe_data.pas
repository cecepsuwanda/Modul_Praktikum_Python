uses crt; 
(* KAMUS *) 
var 
  i : integer; 
  j : word; 
  k : byte; 
  l : real; 
  Shello : string; 
(* ALGORITMA *)  
begin 
  Shello := ‘Hello, dunia’;  
  i:=-1234; 
  j:=60000; 
  k:=3; 
  l:=3.14; 
  writeln(SHello); 
  writeln(‘Nilai i (integer)= ’,i:5); 
  writeln(‘Nilai j (word)   = ’,j:5); 
  writeln(‘Nilai k (byte)   = ’,k:5); 
  writeln(‘Nilai l (real)   = ’,l:5:2); 
  readkey; 
end.
